language: java

install: mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

script:
  - mvn test

deploy:
  provider: script
  script: "cp .travis.m2.settings.xml $HOME/.m2/settings.xml && mvn deploy"
  on:
    tags: true
